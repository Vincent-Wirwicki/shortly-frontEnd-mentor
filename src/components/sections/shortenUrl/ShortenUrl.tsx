import Form from "@/components/form/Form";
import { Button } from "@/components/ui/button";

export default function ShortenUrl() {
  return (
    <section
      className="relative w-full h-fit bg-gray-200 
    flex flex-col justify-end items-center gap-4 pt-20"
    >
      <Form />
    </section>
  );
}
